# Startline Backend v1.0.1

OpenActive harvester + REST API. Pure JavaScript — no native compilation needed.

## Quick start

```bash
npm install
npm start
```

## Deploy to Render

1. Push this folder to GitHub
2. New → Web Service on render.com
3. Connect repo → Deploy

API will be live at your Render URL. Check `/api/health` to confirm.
